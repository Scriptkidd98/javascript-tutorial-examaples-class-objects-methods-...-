//contains a class which is imported in another file
class Door {
  constructor(color, height, width, open, handle) {
    this.color = color;
    this.height = height;
    this.width = width;
    this.open = open;
    this.handle = handle;
  }
}

export default Door;
